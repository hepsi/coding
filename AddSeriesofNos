//Add 1 to 10 values using for loop

#include <iostream>

using namespace std;

int main() {

  int value = 10;
  int sum = 0;
  
  for(i=0; i <=value; i++)
  sum += i;
  
std::cout << "Sum of values 1 to 10 are : " << sum << std::endl;
return 0;
}
